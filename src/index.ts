import { IntensiveQuantity } from "./types/intensiveQuantity";
import { Vertex } from "./types/vertex";

// const vertex = new Vertex({ amount: 100 } as IntensiveQuantity);

export { IntensiveQuantity, Vertex };